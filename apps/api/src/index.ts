/**
 * Placeholder entry point. Replaced by a real NestJS bootstrap at Milestone 4
 * (health-check endpoint, Swagger, structured logging).
 */
import type { AppInfo } from "@mara/shared-types";

export const appInfo: AppInfo = {
  name: "api",
  version: "0.0.0",
};
