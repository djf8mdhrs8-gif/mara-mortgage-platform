import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function LoginScreen() {
  return (
    <PlaceholderScreen
      title="Sign in"
      subtitle="Email/password + biometric login arrives in Milestones 9-11."
    />
  );
}
