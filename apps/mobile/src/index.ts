/**
 * Placeholder entry point. Replaced by a real Expo Router app shell at
 * Milestone 6 (auth stack + tab nav, design tokens, simulator-runnable).
 */
import type { AppInfo } from "@mara/shared-types";

export const appInfo: AppInfo = {
  name: "mobile",
  version: "0.0.0",
};
