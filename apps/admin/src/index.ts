/**
 * Placeholder entry point. Replaced by a real Next.js app shell at
 * Milestone 8 (basic layout, hits the same API).
 */
import type { AppInfo } from "@mara/shared-types";

export const appInfo: AppInfo = {
  name: "admin",
  version: "0.0.0",
};
