# Milestones — Mara Mortgage

> Status: **DRAFT — pending approval**. Ordered easiest/most-foundational → hardest, within each phase. We complete one at a time; each ends in something demoable. Nothing here starts until you approve the planning docs.

Legend: `[ ]` not started · each item lists its **Done when** criteria so "finished" isn't a judgment call.

---

## Phase 0 — Foundation

- [x] **1. Monorepo scaffold** — pnpm workspaces + Turborepo, empty `apps/mobile`, `apps/api`, `apps/admin`, `packages/*` with shared tsconfig/eslint. **Done when**: `pnpm install && pnpm build` succeeds from repo root. ✅ 2026-07-19
- [x] **2. CI pipeline** — GitHub Actions: lint, typecheck, test on every PR. **Done when**: a throwaway PR shows green checks. ✅ 2026-07-19 — verified green on push to main (run 29701380782); PR trigger is configured and runs the identical job.
- [x] **3. Local Docker dev environment** — `docker-compose.yml` for Postgres + Redis, `.env.example`. **Done when**: `docker-compose up` gives a working local Postgres reachable from the API. ✅ 2026-07-19 — verified end-to-end: both containers healthy, Postgres `select 1` OK, Redis `PONG`.
- [x] **4. NestJS API skeleton** — bootstrapped app, health-check endpoint, Swagger/OpenAPI wired up, Pino logging. **Done when**: `GET /api/v1/health` returns 200 with structured logs. ✅ 2026-07-19 — health returns 200 JSON, Swagger UI at /api/docs, pino JSON request logs verified.
- [x] **5. Prisma schema v1** — core entities (User, Application, Document, SavedScenario, LoanProgram, Article, Notification) per `ARCHITECTURE.md` §4, initial migration. **Done when**: `prisma migrate dev` runs clean and Prisma Studio shows the tables. ✅ 2026-07-19 — migration applied, all 7 tables verified in Postgres via psql, and /api/v1/health now checks live DB connectivity (`db: "up"`). Pinned Prisma 6.x; v7 upgrade deferred as a dedicated task.
- [x] **6. Expo app skeleton** — Expo Router navigation shell (auth stack + tab nav placeholders), design tokens (colors/typography from your branding), runs on iOS + Android simulators. **Done when**: app launches to a placeholder home tab on both platforms. ✅ 2026-07-19 — SDK 57 app with (auth) + 4-tab shell and placeholder design tokens; Android production bundle exports cleanly and Metro serves a valid manifest. ⚠️ On-device check pending: scan the QR from `pnpm --filter @mara/mobile dev` with Expo Go (no simulators on this Windows machine; iOS via Expo Go on an iPhone). Placeholder navy/gold palette awaits real branding (Milestone 33).
- [x] **7. Typed API client generation** — OpenAPI spec → generated client, shared via `packages/shared-types`, wired into a React Query provider in the mobile app. **Done when**: the mobile app's home screen displays live data from the `/health` endpoint. ✅ 2026-07-20 — API emits openapi.json (`pnpm --filter @mara/api openapi:generate`), openapi-typescript generates types into shared-types, mobile uses openapi-fetch + React Query; verified in web preview: home screen shows "Connected — API ok · database up" live.
- [x] **8. Admin app skeleton** — Next.js app, basic layout, hits the same API. **Done when**: admin app loads and can call `/health`. ✅ 2026-07-20 — Next.js 15 App Router shell; dashboard server-renders live health ("API connected · status ok · database up") via server-side fetch using shared OpenAPI types. **Phase 0 complete.**

## Phase 1 — MVP

### Auth & identity
- [x] **9. User registration + login (email/password)** — Argon2id hashing, JWT access + refresh token issuance, refresh rotation. **Done when**: a user can register, log out, and log back in from the mobile app. ✅ 2026-07-20 — auth module (register/login/refresh/logout/me), Argon2id, rotating opaque refresh tokens with reuse-detection (replay revokes all sessions), 7 unit tests on the token lifecycle; mobile login/register forms (RHF+zod) + auth gate; full register→logout→login cycle verified in the web preview UI.
- [x] **10. Secure token storage + session persistence** — refresh token in `expo-secure-store`, silent token refresh on app resume. **Done when**: closing and reopening the app keeps the user logged in without a visible flicker to the login screen. ✅ 2026-07-20 — refresh token persisted via expo-secure-store (Keychain/Keystore; localStorage on dev-only web), splash held until silent restore completes, rotation on every restore + foreground resume; verified in web preview: reload lands on Welcome with no login flash, and reload after sign-out correctly requires sign-in.
- [x] **11. Biometric unlock** — Face ID/Touch ID/Fingerprint gate on app resume via `expo-local-authentication`. **Done when**: biometric prompt appears on resume and correctly gates access to the stored session. ✅ 2026-07-20 — lock on cold-start restore + background/resume, auto-prompt with lock screen fallback (Unlock / Sign out instead), skipped cleanly when no biometrics (web/unenrolled devices); fresh logins don't re-prompt. Android bundle exports with the native module; web regression suite passes. ⚠️ The OS biometric prompt itself can only fire on a real device — verify via Expo Go once the phone-side install is sorted.
- [x] **12. Role-based access control** — borrower/realtor/loan_officer/admin roles, NestJS guards, row-level ownership checks on borrower data. **Done when**: a borrower account cannot fetch another borrower's application via direct API call (tested, not assumed). ✅ 2026-07-20 — @Roles decorator + RolesGuard, minimal applications module with row-level ownership (others' apps return 404, not 403, to avoid existence leaks). Live-tested: borrower B got 404 on borrower A's app and [] on list; unauthenticated → 401; borrower PATCH status → 403; promoted loan officer → full list + status update 200. 7 new unit tests (16 total).

### Calculation engine (business-critical — built and tested in isolation before any UI)
- [x] **13. `packages/mortgage-calc`: amortization core** — principal & interest, full amortization schedule generation. **Done when**: unit tests match a known-correct reference amortization table to the cent. ✅ 2026-07-20 — integer-cents engine (monthlyPayment + buildAmortization) matches independently computed Python-Decimal references to the cent across 6 scenarios (incl. classic $100k@6%/30y = $599.55 and 0% edge case), plus row-level invariant checks and a 60-combination sweep. 15 tests.
- [x] **14. Basic mortgage calculator (UI)** — purchase price, down payment ($/%), rate, term, taxes, insurance, HOA, PMI, closing costs → monthly payment, cash to close, loan amount. **Done when**: results match Karl's Mortgage Calculator for 3 test scenarios you supply. ✅ 2026-07-20 — calculateBasicMortgage (PITI+PMI+cash-to-close) in mortgage-calc with 5 new tests pinned to Python-Decimal references; live calculator screen (calculators tab → Mortgage Payment) with $/% down toggle and instant recompute. All 3 reference scenarios verified in the running UI to the cent (user supplied no custom scenarios; standard-formula references used — P&I values match published tables). PMI auto-drops at LTV ≤ 80.
- [x] **15. Amortization schedule UI + PDF export** — interactive schedule table, PDF generation (server-side), share sheet. **Done when**: a generated PDF opens correctly on both iOS and Android and matches on-screen numbers. ✅ 2026-07-21 — virtualized 360-row schedule screen linked from the calculator (row 1 matches reference to the cent); POST /calculators/amortization/pdf recomputes server-side via the shared engine and streams a real PDF (verified: %PDF binary, contains $1,264.14 / $255,085.82, 401 unauthenticated); Export button verified in UI (web download). Native share-sheet path (expo-file-system + expo-sharing) built but ⚠️ device-open check pending Expo Go on a real phone.
- [x] **16. Extra-payment calculator** — extra monthly/annual/one-time payments → years saved, interest saved, new payoff date, updated schedule. **Done when**: verified against a manual spreadsheet calculation for 2 scenarios. ✅ 2026-07-21 — buildExtraPaymentPlan (extends the core loop via per-month extra callback — no duplicated schedule logic) with 5 tests; UI shows original-vs-extras comparison, time/interest saved, and calendar payoff date; schedule screen + PDF accept extras (savings line in header). Both reference scenarios verified in the live UI to the cent: $200/mo → 9yr 2mo / $90,074.66 saved; combo ($100/mo + $1k/yr + $10k@24) → 8yr 2mo / $140,035.84 saved.
- [x] **17. Refinance calculator** — current vs. new mortgage comparison, monthly savings, break-even point, total cost comparison. **Done when**: break-even math verified against a manual calculation. ✅ 2026-07-21 — calculateRefinance (cash or financed closing costs; null break-even when payment rises) + 4 tests; side-by-side UI with honest outputs (shows lifetime ADDED cost when a term reset outweighs the lower payment, with a shorter-term hint). Break-even manually verified: ceil($6,000 / $308.19) = 20 months, matched in UI. Bonus: found+fixed a 1-cent float rounding edge in per-month interest — engine now uses exact BigInt half-up division, validated against exact-rational (Fraction) references.
- [x] **18. Affordability calculator** — income, debts, down payment, taxes, HOA, rate → max home price, estimated payment (standard DTI-based approach). **Done when**: output matches a documented DTI-ratio reference calculation. ✅ 2026-07-21 — calculateAffordability (28/36 rule, price-scaled property tax inverted analytically, price floored conservatively) + 5 tests; UI shows headline max price, binding-ratio explanation, PITI+HOA breakdown, and both budget caps. Both references verified in live UI: A1 $323,443 (front-end limited, total $2,239.99 vs $2,240 cap), A2 $360,546 (back-end limited, exactly $2,700.00). **Phase 1 core calculator suite (13–18) complete.**

### Arive integration
- [ ] **19. Arive adapter interface + WebView implementation** — ⏸️ *deferred 2026-07-21 at user request; plan-A is the portal-handoff adapter. Waiting on: Mara's borrower application link from Arive's POS App Config page (screenshot reviewed: User Settings → API Integration is UWM-lender-specific, not a developer API), and/or Arive support's answer on API/SSO availability.* — `AriveAdapter` interface per `ARCHITECTURE.md` §3; `AriveWebViewAdapter` generates a secure deep-link/SSO URL into the existing borrower portal. **Done when**: a logged-in borrower can tap "Start Application" and land inside Arive's portal without re-entering credentials (or with a documented, minimal handoff if SSO isn't available). **Blocked on**: your confirmation of what Arive actually exposes (see `PROJECT_PLAN.md` risk register) — this milestone's exact shape depends on that answer.
- [x] **20. Application status tracking (basic)** — application record created on start, status field, borrower-visible progress screen. **Done when**: an application's status updates (manually via admin for now) and reflects correctly in the borrower's app. ✅ 2026-07-21 — "My Loan" tab: empty state → Start my application (creates DRAFT), 7-step status timeline with friendly per-step descriptions, CANCELLED handled, pull-to-refresh + 30s polling. Verified live: loan-officer PATCH to UNDERWRITING reflected in the borrower's timeline (✓✓✓ through Processing, Underwriting current). Admin UI for status changes arrives with Milestone 28-30; staff API is the manual path meanwhile.

### Documents
- [x] **21. Secure document upload** — `expo-document-picker`/`expo-image-picker` → S3/R2 via signed upload URLs, document list per application. **Done when**: a borrower can upload a PDF/photo and see it listed, and the file is confirmed present in storage. ✅ 2026-07-21 — StorageService adapter (local disk now; S3/R2 swaps in behind the same interface at deploy — multipart-to-API replaces signed URLs until then), upload/list/download endpoints with row-level ownership (404s for other borrowers), 15MB cap + PDF/image whitelist, sanitized filenames with server-generated UUID keys, path-traversal guard; 6 service tests. Mobile: Documents section on My Loan (picker → multipart upload, status chips). Verified live: upload 201 → file on disk → listed in UI → download bytes identical → Alice 404s → .exe rejected 400.
- [x] **22. Document status + loan-officer visibility** — admin/loan-officer view of uploaded documents, mark as reviewed/needs-resubmission. **Done when**: a status change made in admin is visible to the borrower. ✅ 2026-07-21 — PATCH /documents/:id/status (LOAN_OFFICER/ADMIN only; borrower attempt → 403), staff already see all docs via list/download from M21; borrower Documents list polls every 30s and shows a "please upload a new copy" hint on NEEDS_RESUBMISSION. Verified live: LO set NEEDS_RESUBMISSION → borrower UI showed the red chip + hint. Admin dashboard UI for this arrives in M28-30.

### Content & contact
- [x] **23. Loan programs content (static + admin-editable)** — Conventional/FHA/VA/USDA/Jumbo/etc. educational screens, content sourced from admin dashboard. **Done when**: editing a loan program in the admin app updates the mobile app's content without a mobile app release. ✅ 2026-07-21 — loan-programs API (list/detail for users; ADMIN-only create/update; drafts hidden from borrowers) + idempotent seed with all 11 programs from the spec; Learn tab lists programs with detail screens and a contact CTA. Verified live: borrower PATCH → 403; admin PATCH title → appeared in the app on next load, zero release. Admin *UI* for editing closes the loop in Milestone 29 (admin auth lands in 28).
- [x] **24. Contact section** — call/text/email deep links, "request pre-approval" form, schedule-appointment link (Calendly-style embed or link-out, decided at implementation time). **Done when**: each action correctly opens the native call/text/email/calendar flow. ✅ 2026-07-22 — Contact tab with Mara's real details ((954) 612-5535, missa@certifiedhomeloans.com) in one config file; Call/Text/Email rows, Schedule (opens EXPO_PUBLIC_SCHEDULE_URL when set, else pre-filled email), and a pre-approval CTA that composes a templated email greeting with the signed-in user's name. Verified in web preview by intercepting window.open: sms/mailto/schedule/pre-approval URLs all exact; tel: can't fire in the sandboxed browser (no handler) but is built from the same verified E.164 constant — ⚠️ tap-check on a real phone with the other Expo Go items.
- [x] **25. Compliance content slots** — NMLS #, Equal Housing Lender notice, state disclosures rendered from admin-editable content. **Done when**: content you/legal supply renders correctly on the relevant screens; flagged as **blocked on your compliance content**, not an engineering task. ✅ 2026-07-22 — ContentBlock key-value store (public GET so pre-auth screens can render it; ADMIN-only PUT, 401/403 verified); login-screen footer + Contact-tab footer + Licensing & Disclosures screen, all DB-served with offline fallbacks. Seeded with Company NMLS #1806779 (from certifiedhomeloans.com), Equal Housing Lender statement, estimates-only disclaimer, and a clearly marked **[PLACEHOLDER]** for state licensing list — ⚠️ final wording still needs Mara/compliance review; re-seeds never overwrite admin edits.

### Notifications
- [x] **26. Push notification plumbing** — FCM setup, device token registration, backend send capability. **Done when**: a test notification sent from the API (or a temporary script) reaches a real device on both iOS and Android. ✅ 2026-07-22 (structurally) — PushToken registration (format-validated, device reassignment on account switch), NotificationsService records Notification rows and delivers via expo-server-sdk with per-device outcomes; POST /notifications/test smoke endpoint; mobile registers tokens silently after sign-in (permissions, Android channel, EAS projectId aware). Verified against Expo's LIVE push API: fake token → DeviceNotRegistered flowed through the whole pipeline correctly; no-devices and bad-format paths verified. ⚠️ Actual device receipt (the literal done-when) pending Expo Go on a real phone + EAS project setup — first item on the device checklist.
- [x] **27. Document-reminder + application-milestone notifications** — triggered server-side on relevant events. **Done when**: uploading a document or an admin status change triggers the correct notification on a test device. ✅ 2026-07-22 — three event triggers, all best-effort (push failure never breaks the mutation): application status change → LOAN_MILESTONE to the borrower with friendly per-status copy (no-op changes don't re-notify); document → NEEDS_RESUBMISSION → DOCUMENT_REMINDER naming the file (no duplicates); borrower upload → GENERAL to all staff except the uploader. Verified live via DB rows for all three (jacob got milestone+reminder; bob AND admin got the upload alert). Device receipt rides on the M26 Expo Go/EAS checklist. 37 API tests.

### Admin (minimal, Phase 1 scope)
- [ ] **28. Admin auth + basic dashboard shell** — separate stricter-session login for admin app. **Done when**: only `admin` role can log into `apps/admin`.
- [ ] **29. Admin: content management** — CRUD for loan programs and articles. **Done when**: content created/edited in admin appears correctly in the mobile app.
- [ ] **30. Admin: send push notification** — compose + send to all users or a segment. **Done when**: a notification sent from admin reaches a test device.

### Phase 1 hardening & ship
- [ ] **31. Security pass** — rate limiting on auth endpoints, input validation audit, dependency scan clean, audit logging on document access. **Done when**: checklist in `ARCHITECTURE.md` §6 is verified item by item.
- [ ] **32. Error tracking + crash reporting** — Sentry wired into mobile + API. **Done when**: a deliberately triggered test error appears in Sentry within a minute.
- [ ] **33. App icons, splash screens, store assets** — branding applied throughout, EAS build config finalized. **Done when**: a release build installs cleanly from EAS on a physical device.
- [ ] **34. TestFlight + Play internal testing submission** — first real external-facing milestone. **Done when**: you can install the app on your own phone via TestFlight/Play internal testing link. **Requires your explicit go-ahead before submission** (app store submission is a real-world action, not a routine one).

---

## Phase 1.5 — Rounding out the MVP

- [ ] **35. Rent vs. buy calculator** — projected wealth comparison over time, chart visualization.
- [ ] **36. Buydown calculator** — permanent, 2-1, 3-2-1 buydowns; payment schedule + savings.
- [ ] **37. Property analysis tool** — address + financials → instant payment calculation.
- [ ] **38. Save & compare mortgage scenarios** — save calculator runs, side-by-side comparison view.
- [ ] **39. Realtor tools** — quick calculator mode, save favorite scenarios, send scenario directly to a client (email/text with PDF).
- [ ] **40. Saved favorites** — properties and calculations, accessible from user profile.
- [ ] **41. Secure borrower ↔ loan officer messaging** — in-app thread, push-notified.
- [ ] **42. Expanded notification types** — rate updates, educational tips, closing reminders; admin-schedulable.
- [ ] **43. Admin analytics dashboard** — usage metrics (PostHog integration), key funnel views (calculator use → pre-approval request → application start).
- [ ] **44. Admin calculator management** — enable/disable calculators, edit default assumptions (e.g. default PMI rate).
- [ ] **45. Public app store launch** — full review cycle, public listing live. **Requires your explicit go-ahead.**

---

## Phase 2 — Growth features (not sequenced yet)

Tracked in `ROADMAP.md` Phase 2; will be broken into milestones after Phase 1.5 ships and we can prioritize using real usage data rather than guessing now.

---

## Working agreement

- We complete milestones **in order**, one at a time, and I'll show you the result before starting the next.
- Any milestone touching real-world state (app store submission, sending a real push notification broadly, deploying to production, spending money on a paid service) gets an explicit confirmation from you first, even if it's "next" in this list.
- If a milestone turns out to be bigger than it looks once we're in it, I'll say so and propose splitting it rather than quietly ballooning scope.
